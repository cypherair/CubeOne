/// CubeKit — the cube model and solver engine for Cube One.
///
/// Pure Swift, no UI dependencies. The app target renders and animates;
/// everything about cube state, legality, and solving lives here.
public enum CubeKitInfo {
    public static let version = "0.1.0"
}
